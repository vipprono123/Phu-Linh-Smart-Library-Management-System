
export const SERVER_URL = process.env.NEXT_PUBLIC_SERVER_API_URL;

// const meta = {
//   title: 'app',
//   description: 'The descriptions.',
// };

export default function getMetadata() {
  // const data = cloneObj(meta);
  // if (title) {
  //   data.title = meta.title + ' | ' + title;
  // }
  return null;
}
